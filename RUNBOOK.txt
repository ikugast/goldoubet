金豆芽 免费稳定运行步骤

1. 启动后端
   cd /Users/bytedance/Downloads/jindouya_backend_seedai
   ./start_backend.sh

2. 新开一个终端，启动公网隧道
   cd /Users/bytedance/Downloads/jindouya_backend_seedai
   ./start_tunnel.sh

3. 复制 cloudflared 输出的新 trycloudflare 地址

4. 修改前端文件
   文件：/Users/bytedance/Downloads/jindouya_backend_seedai/index.html
   把 API_BASE 改成新的 trycloudflare 地址

5. 更新记录文件
   文件：/Users/bytedance/Downloads/jindouya_backend_seedai/CURRENT_BACKEND_URL.txt
   写入新的 trycloudflare 地址

6. 重新上传 index.html 到 Cloudflare Pages

7. 打开页面检查
   前端：https://aidragon.pages.dev/
   后端连通检查：/api/admin/ai/ping
   决策接口：/api/strategies/decision
   研究接口：/api/research/generate

8. 注意
   trycloudflare 地址不是永久的
   一旦 tunnel 变了，就必须同步更新 index.html 并重新上传 Pages

[Supabase scheduler verification]
- job_finalize_pending_scheduler_jobs_every_minute: active=true, runs every minute, cron execution succeeded
- stock_steady_intraday cron: active=true, runs every 5 minutes, cron execution succeeded
- manual invoke test:
  select public.invoke_scheduler_job('stock_steady_intraday');
  -> request_id = 6
- finalize test:
  stock_steady_intraday log finalized successfully
  run_status = success
- conclusion:
  Supabase scheduler -> backend -> finalize callback chain is working
  stock cron may produce no logs outside trading-session condition, which is expected


  [2026-03-23 stock scheduler final state]
- scheduler job key renamed from stock_steady_intraday to stock_steady
- cron renamed from job_stock_steady_intraday to job_stock_steady
- cron schedule: */5 * * * *
- invoke target: public.invoke_stock_scheduler_job_if_open('stock_steady')
- stock decision context now includes:
  - live A-share quote data
  - live market news feed
  - strategy snapshot positions/account
  - strategy style constraints
- backend_base_url updated to current Cloudflare tunnel
- CURRENT_BACKEND_URL.txt updated to current Cloudflare tunnel